name: Release

permissions: read-all

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    if: github.repository == 'zebodotdev/inttegro-sdk-typescript'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: release
    env:
      INTTEGRO_OPENAPI_SPEC: openapi/commerce.yml
    permissions:
      attestations: write
      contents: write
      id-token: write

    steps:
      - name: Check out the release commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Check out the OpenAPI contract
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: zebodotdev/openapi
          ref: 7f9e846dc4202e2abb75733e99c69e4fd5b86fae
          path: openapi
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24.20.0"
          package-manager-cache: false
          registry-url: "https://registry.npmjs.org"

      - name: Verify the release tag and package version
        shell: bash
        run: |
          set -euo pipefail
          package_version="$(node --print "require('./package.json').version")"
          test "${GITHUB_REF_NAME}" = "v${package_version}"
          test "$(git rev-list -n 1 "${GITHUB_REF_NAME}")" = "${GITHUB_SHA}"

      - name: Install, test, and build
        run: |
          npm ci
          npm run format:check
          npm run lint
          npm run typecheck
          npm test
          npm run build

      - name: Pack the exact release artifact
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          npm pack --pack-destination artifacts
          tar -tzf artifacts/*.tgz | sort > artifacts/PACKAGE-CONTENTS.txt
          grep -q 'package/dist/index.js' artifacts/PACKAGE-CONTENTS.txt
          grep -q 'package/src/index.ts' artifacts/PACKAGE-CONTENTS.txt

      - name: Attest the package
        id: attest
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: artifacts/*.tgz

      - name: Record release provenance and checksums
        shell: bash
        run: |
          set -euo pipefail
          cp "${{ steps.attest.outputs.bundle-path }}" artifacts/ATTESTATION.sigstore.jsonl
          {
            echo "repository=https://github.com/${GITHUB_REPOSITORY}"
            echo "tag=${GITHUB_REF_NAME}"
            echo "commit=${GITHUB_SHA}"
            echo "workflow=${GITHUB_WORKFLOW_REF}"
          } > artifacts/PROVENANCE.txt
          (
            cd artifacts
            sha256sum ./*.tgz ./ATTESTATION.sigstore.jsonl ./PACKAGE-CONTENTS.txt ./PROVENANCE.txt > SHA256SUMS
          )

      - name: Create the draft GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "${GITHUB_REF_NAME}" artifacts/*
          --draft
          --generate-notes
          --title "Inttegro TypeScript SDK ${GITHUB_REF_NAME}"
          --verify-tag

      - name: Publish the exact artifact to npm with OIDC
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        shell: bash
        run: npm publish ./artifacts/*.tgz --access public --ignore-scripts --provenance

      - name: Finalize the immutable GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release edit "${GITHUB_REF_NAME}" --draft=false
